import { PhotoContainer, UserIcon, SelectPhotoInput } from "./Photo.styled";

export const Photo = () => {
  return (
    <PhotoContainer>
      <SelectPhotoInput type="file" name="" id="" />
      <UserIcon />
    </PhotoContainer>
  );
};
